// JS
import baseline from 'baseline/baseline';
import responsiveMenus from 'baseline/responsive-menus';
import stickyHeader from 'baseline/sticky-header';
import flexslider from 'flexslider/jquery.flexslider-min';

// CSS && SCSS
require('../scss/theme-header.scss'); // used by WordPress to identify the theme
require('../baseline/node_modules/flexslider/flexslider.css'); // default flexslider styles
require('../scss/style.scss'); // theme overrides for Baseline styles
